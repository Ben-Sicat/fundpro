"""Domain models and reference data.

The models mirror `frontend/lib/types.ts` field for field — that file is the
contract the UI already codes against, so the JSON this service emits must
match it exactly (camelCase included) for the seam swap to be a no-op.
"""
